Jan Sevak Centralized System:

  --Web framework for handling HTTP requests and responses
      Flask==3.0.0
  
  --Library for making HTTP requests (used for API calls, including Telegram bot and Phyphox)
      requests==2.31.0
  
  --MySQL connector for interacting with the MySQL database
      mysql-connector-python==8.2.0
  
  --Python wrapper for the Telegram Bot API (used for accident and ambulance bots)
      pyTelegramBotAPI==4.14.0
  
  --Threading module for running multiple bots simultaneously
      threading; python_version>="3.0"
  
  --Open-source libraries and APIs used:
      Phyphox (https://phyphox.org) for retrieving sensor data from smartphones
      Telegram Bot API (https://core.telegram.org/bots/api) for bot-based notifications

Ambulance Interface Code:

  --Flask==2.0.1
  --python-telegram-bot==13.7
  --requests==2.26.0
  --geopy==2.2.0
  --gunicorn==20.1.0
  --Flask-Cors==3.0.10
